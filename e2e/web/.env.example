# Neon admin/owner connection string — same value used for `pnpm migrate` and e2e/backend/.env.
# Needed to recover verification/invitation codes and to clean up test data after each run.
DATABASE_URL="postgresql://USER:PASSWORD@ep-xxxx-pooler.eu-west-1.aws.neon.tech/finance?sslmode=require"
